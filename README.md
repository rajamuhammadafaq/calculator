# Getting Started to compare Air Quality app
    - Open the terminal and enter npm i to install the require dependencies
    - Use  npm run start to start the server, by default it will opens up at 3000 port
